/*import { scrapeNews } from '../../api/scrape.js';

export default {
  async load() {
    const updateNews = async () => {
      const news = await scrapeNews();
      this.data.news = news;
    };

    await updateNews();
    setInterval(updateNews, 60000);
  }
};*/
